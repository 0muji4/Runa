package com.runa.android.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Runa design tokens. IDENTICAL to the iOS design system. Dark theme only.
 * Moon motif: a soft moonlight-pink accent over near-black surfaces.
 */
object RunaColors {
    val Background = Color(0xFF0E0E12)
    val Surface = Color(0xFF16161C)
    val Heading = Color(0xFFF5F3EF)
    val Body = Color(0xFFC8C6CE)
    val Subtle = Color(0xFF9A9AA5)

    /** Moonlight accent. */
    val Accent = Color(0xFFF4A9C0)
    val SubAccent = Color(0xFFE8E2D0)
}
